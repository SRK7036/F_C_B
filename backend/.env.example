# --- Supabase Postgres (direct SQLAlchemy URL) ---
# Example: postgresql+psycopg://postgres:YOUR_PASSWORD@YOUR_HOST:5432/postgres
SUPABASE_DB_URL=postgresql+psycopg://postgres:password@db.host:5432/postgres

# --- CORS ---
CORS_ORIGINS=http://localhost:3000

# --- RAG / Embeddings ---
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
PERSIST_DIR=./chroma_db
KB_DIR=../knowledge_base

# --- LLM selection ---
# Options: anthropic | openai
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- Crew toggle (optional) ---
USE_CREW=false

# --- Email (prototype sends to console if SMTP creds missing) ---
EMAIL_FROM="Advisor <noreply@example.com>"
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
